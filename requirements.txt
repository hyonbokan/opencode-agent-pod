# Runtime dependencies of the engine (agent/) and the HTTP + SSE pod service (pod/).
httpx>=0.27
pydantic>=2.7
psutil>=5.9  # detached child-process reaping in agent/opencode/driver.py
fastapi>=0.115  # the pod service (pod/app.py)
uvicorn>=0.30  # ASGI server for `python -m pod`
